import { FilterGestionViajePipe } from './filter-gestion-viaje.pipe';

describe('FilterGestionViajePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGestionViajePipe();
    expect(pipe).toBeTruthy();
  });
});
