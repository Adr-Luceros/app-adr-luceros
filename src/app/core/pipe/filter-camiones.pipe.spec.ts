import { FilterCamionesPipe } from './filter-camiones.pipe';

describe('FilterCamionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCamionesPipe();
    expect(pipe).toBeTruthy();
  });
});
