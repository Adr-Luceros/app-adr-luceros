import { FilterRolCargoPipe } from './filter-rol-cargo.pipe';

describe('FilterRolCargoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRolCargoPipe();
    expect(pipe).toBeTruthy();
  });
});
