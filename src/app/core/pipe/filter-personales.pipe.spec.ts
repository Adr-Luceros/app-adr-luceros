import { FilterPersonalesPipe } from './filter-personales.pipe';

describe('FilterPersonalesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPersonalesPipe();
    expect(pipe).toBeTruthy();
  });
});
